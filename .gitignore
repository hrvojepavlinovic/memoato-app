.wasp/
node_modules/
imports/
__pycache__/
*.pyc
*.pyo

# Local Cloudflare tunnel config (contains tunnel id + credentials path)
deploy/cloudflared-memoato.yml

# Production release artifacts
deploy/releases/
deploy/current


# Ignore all dotenv files by default to prevent accidentally committing any secrets.
# To include specific dotenv files, use the `!` operator or adjust these rules.
.env
.env.*

# Don't ignore example dotenv files.
!.env.example
!.env.*.example
